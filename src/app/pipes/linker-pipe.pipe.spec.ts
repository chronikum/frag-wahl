import { LinkerPipePipe } from './linker-pipe.pipe';

describe('LinkerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
